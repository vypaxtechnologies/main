import { useSEO } from '@/hooks/useSEO';
import { Link } from 'react-router-dom';
import { ArrowRight, Briefcase } from 'lucide-react';
import SectionHeading from '@/components/SectionHeading';
import Breadcrumbs from '@/components/Breadcrumbs';
import { careers } from '@/data/faq';
export default function CareersPage() { useSEO({ title: 'Careers | Vypax Technologies', description: 'Build your future with Vypax Technologies. We are building a team that loves technology, creativity and meaningful business impact.' }); return <><section className="pt-32 pb-12 md:pt-40"><div className="container-x"><Breadcrumbs items={[{ label: 'Home', path: '/' }, { label: 'Careers' }]} /><SectionHeading eyebrow="Join Us" title="Build Your Future With Us" subtitle="We're building a team that loves technology, creativity and meaningful business impact." center={false} /></div></section><section className="section-pad bg-[var(--bg-subtle)]"><div className="container-x max-w-3xl space-y-4">{careers.map((role) => <div key={role.id} className="flex flex-col justify-between gap-4 rounded-card border border-[var(--border)] bg-[var(--card)] p-6 shadow-card sm:flex-row sm:items-center"><div className="flex items-start gap-4"><div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-xl bg-brand-gradient text-white"><Briefcase className="h-5 w-5" /></div><div><h3 className="font-semibold text-navy-900 dark:text-white">{role.title}</h3><p className="mt-1 text-sm text-[var(--text-muted)]">{role.type} · {role.location}</p><p className="mt-2 text-sm text-[var(--text-muted)]">{role.description}</p></div></div><Link to="/contact" className="btn-primary shrink-0">Apply Now <ArrowRight className="h-4 w-4" /></Link></div>)}</div></section></>; }
