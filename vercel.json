{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "https://vypax-api.onrender.com/api/$1" },
    { "src": "/(.*)", "dest": "dist/$1" }
  ]
}